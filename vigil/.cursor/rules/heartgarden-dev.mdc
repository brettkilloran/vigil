---
description: Pointer to canonical vigil/ Cursor rule (avoid duplicate instructions)
globs: vigil/**
alwaysApply: false
---

**Canonical guardrails** for this app live in **`.cursor/rules/heartgarden-app.mdc`** at the **repository root** (same `vigil/**` globs). Read that file plus **`vigil/AGENTS.md`** before changing dev scripts, Storybook, or CSS layout.

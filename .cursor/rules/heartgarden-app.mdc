---
description: heartgarden Next.js app — dev and Storybook guardrails (app directory is vigil/)
globs: vigil/**
alwaysApply: false
---

When working under the **heartgarden** app (folder **`vigil/`** in git — see `vigil/docs/NAMING.md`), read **`vigil/AGENTS.md`** → **"Local dev, Node, and Storybook (guardrails)"** before changing dev scripts, Storybook, or CSS-module vs global token layout.
